{
    "name": "autoyield-dao",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "coverage": "npx hardhat coverage",

        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:factory": "hardhat run scripts/deployFactory.js --network hardhat",
        "deploy:vault": "hardhat run scripts/deployVault.js --network hardhat"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "chai": "^4.5.0",
        "dotenv": "^16.4.5",
        "ethers": "^6.15.0",
        "hardhat": "^2.26.3"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^4.9.6"
    }
}
